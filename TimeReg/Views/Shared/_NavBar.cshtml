<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            @Html.ActionLink("Rep It", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li>@Html.ActionLink("Home", "Index", "Home")</li>
                <li>@Html.ActionLink("Time Registration", "Index", "TimeRegistrations")</li>
                <li>@Html.ActionLink("Weekly Comment", "Index", "Comments")</li>
                <li>@Html.ActionLink("Overview", "Index", "Overview")</li>
            
            </ul>
            @using Microsoft.AspNet.Identity
            @if (Request.IsAuthenticated)
            {
            @Html.Action("UserPartial", "WindowsAuth", new { ZId = User.Identity.GetUserName() })
            }

            @*Replaced by above line to use a controller*@
            @*@Html.Partial("_LoginPartial")*@
        </div>
    </div>
</div>
